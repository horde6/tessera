<?xml version="1.0"?>
<configuration>
 <configsection name="general">
  <configheader>General Settings</configheader>
   <configstring name="name" desc="What site name should we use?" />
   <configswitch name="mode" desc="When should we use one-time-passwords (OTP)?">enable
   <case name="disable" desc="Disable OTP" />
   <case name="enable" desc="Enable OTP for users who configured it" />
   <case name="require" desc="Require OTP for all users" />
  </configswitch>
 </configsection>
 <configsection name="storage">
  <configheader>Storage System Settings</configheader>
  <configswitch name="driver" desc="What storage driver should we use?">sql
   <case name="sql" desc="SQL">
    <configsection name="params">
     <configsql switchname="driverconfig">
     </configsql>
    </configsection>
   </case>
  </configswitch>
 </configsection>
</configuration>
